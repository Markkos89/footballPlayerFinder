import React from 'react';
import { shallow } from 'enzyme';
import Header from '../footballPlayerFinder/components/Header.js';

describe('Header component', () => {

	it("it should render without errors", () => {
		expect(1).toBe(1);
	});
});